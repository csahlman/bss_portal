<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Boston Startup School" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

<!--     <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid"> -->
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="row-fluid"> 
          <div class="span12">
            <div class="span3 pagination-centered">
              <br>
              <%= link_to image_tag("bsslogo.png"), root_path %>
              <br>
            </div>
            <div class="span9 pagination-centered">
              <div class="span2 pagination-centered">
                <p class="lead">
                  <br>
                  <%= link_to "Development", root_path(track: "Development") %>
                </p>
              </div>
              <div class="span2 pagination-centered">
                <p class="lead">
                  <br>
                  <%= link_to "Product", root_path(track: "Product") %>
                </p>
              </div>
              <div class="span2 pagination-centered">
                <p class="lead">
                  <br>
                  <%= link_to "Marketing", root_path(track: "Marketing") %>
                </p>
              </div>
              <div class="span2 pagination-centered">
                <p class="lead">
                  <br>
                  <%= link_to "Sales", root_path(track: "Sales") %>
                </p>
              </div>
              <div class="span4">
                <br>  
                <% if user_signed_in? && current_user.admin? %>
                  <%= link_to 'Admin', '/admin' %><br>
                <% end %>
                <% if user_signed_in? %>
                  <%= link_to "Sign Out", logout_path, method: :delete %><br>
                  <%= link_to "Edit your account", edit_user_path(current_user) %>
                <% end %>

              </div>                                         
            </div>
          </div>         
<!--           <div class="container-fluid nav-collapse">
            <ul class="nav">
              <% if user_signed_in? && current_user.admin? %>
              <li><%= link_to "Admin", "/admin"  %></li>
              <% end %>
              <% if user_signed_in? %>
              <li><%= link_to "Sign Out", logout_path, method: :delete  %></li>
              <% else %>
              <li><%= link_to "Sign In", sign_in_path %></li>
              <% end %>
            </ul>
          </div> -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
          <%= bootstrap_flash %>
          <%= yield %>
      </div><!--/row-->

      <footer>
        <h3>&copy; Boston Startup School 2012</h3>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <% if Rails.env.development? %>
    <pre><%= params.inspect %></pre>
    <% end %>

  </body>
</html>
